import Header from "../components/Header";
import History from "../components/History";

const Details = () => (
    <div>
        <Header theme='light' />
        <History />
    </div>
)

export default Details;